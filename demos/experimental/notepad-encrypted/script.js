// < ========================================================
// < Imports
// < ========================================================

import { TabSwitcher } from "./tab-switcher.js";

// < ========================================================
// < Functionality
// < ========================================================

class Note {
    constructor(uuid) {
        
    }
}

// < ========================================================
// < Entry Point
// < ========================================================

function main() {

    let switcherID = TabSwitcher.create('page', 'switcher');
    let switcher = new TabSwitcher(switcherID);

    // Make a note, be able to store it
    // Be able to load a note

}

// < ========================================================
// < Execution
// < ========================================================

main();